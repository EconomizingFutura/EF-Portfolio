import React from "react";
import Header from "../sections/Header";

const Pricing: React.FC = () => {
  return (
    <div>
      <Header background="" handleShowForms={() => console.log("hello")} />
      pricing
    </div>
  );
};

export default Pricing;
